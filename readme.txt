
Viewing vigor through index.html causes an error by "type=module" in index.html.
must open through vscode live
=== update ====
The current UI is broken because the database is not used anymore so it cannot pull user data from Google Firebase
